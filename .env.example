# libmssdk 配置示例。
#
# 你必须提供自己安装的汽水音乐客户端里的两个原生文件：
#   bdms.node
#   metasecml.dll
# 它们不在本仓库中，也不要再分发。

# —— 必需：Windows/Docker 部署时提供客户端目录或模块路径 ——
# 指向汽水客户端版本目录（目录里应能看到 resources/）
LIBMSSDK_CLIENT_DIR=C:\Users\<you>\AppData\Local\Programs\Soda Music\3.7.0
# 或直接指定 bdms.node；同目录必须存在 metasecml.dll
# LIBMSSDK_BDMS_MODULE=C:\path\to\bdms.node

# —— 建议设置：服务鉴权（等价于账号取流能力，别裸奔）——
LIBMSSDK_TOKEN=change-me-to-a-random-token

# 监听地址/端口（默认 127.0.0.1:8899，跨机请走 SSH 隧道）
# LIBMSSDK_BIND=127.0.0.1
# LIBMSSDK_PORT=8899

# 身份文件目录（默认 <仓库>/data；服务以 SYSTEM 等只读身份跑时可指明可写目录）
# LIBMSSDK_STATE_DIR=C:\libmssdk\data

# —— 覆盖项，通常不需要 ——

# 钉死设备指纹（不设则用自动生成并落盘的那一份）
# 注意：必须与调用方 URL 里的 device_id / fp、以及会话 cookie 同设备
# LIBMSSDK_DEVICE_ID=
# LIBMSSDK_IID=
# LIBMSSDK_FP=

# 允许无 token 访问（仅本机调试用）
# LIBMSSDK_ALLOW_ANONYMOUS=1

# 上游超时 / 请求体上限
# LIBMSSDK_UPSTREAM_TIMEOUT_MS=15000
# LIBMSSDK_MAX_BODY_BYTES=262144
